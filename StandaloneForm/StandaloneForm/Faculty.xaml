<Window x:Class="StandaloneForm.Faculty"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Faculty" ResizeMode="NoResize" Name="FacultyWindow" >
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="2 5 2 5" />
        </Style>
    </Window.Resources>
    <StackPanel>
        <Grid Name="ListOfFaculties">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="50" />
            </Grid.ColumnDefinitions>
        </Grid>
        <WrapPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Ок" Width="50" Height="25" HorizontalAlignment="Right" Click="ButtonOk_Click" />
            <Button Content="Обновить" Width="75" Height="25" HorizontalAlignment="Right" Click="ButtonReset_Click" />
            <Button Content="Отмена" Width="50" Height="25" HorizontalAlignment="Right" Click="ButtonCancel_Click" />
        </WrapPanel>
    </StackPanel>
</Window>
